//import java.util.ArrayList;

/**
 * @deprecated We plan to rewrite the system based on reasoning rather than
 * truth value assignment, so this class is deprecated.
 * */
@SuppressWarnings("CommentedOutCode")
public class Axiom {
//    public static PExpression makeAxiom(PExpression pattern, PExpression...pExps) {
//        for (int i = 0; (i < pExps.length) || (i < pattern.getSymbolTypeMax()); i++) {
//            try {
//                pattern = pattern.setSymbolToNewExpression(i, pExps[i]);
//            } catch (Exception e) {
//                continue;
//            }
//        }
//        return pattern;
//    }
}
